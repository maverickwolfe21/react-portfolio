// This will have my name, a small picture, and will conatin another component with the navbar.
import Navigation from "../components/Navigation.jsx";

export default function Header() {
  return <Navigation />;
}
